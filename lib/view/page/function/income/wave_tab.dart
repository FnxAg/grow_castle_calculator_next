import 'package:flutter/material.dart';

/// 收入来源「推波」tab：输入框 + 实时计算详单（待补充）
class WaveTab extends StatelessWidget {
  const WaveTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('推波（待实现）', style: TextStyle(color: Colors.grey)),
    );
  }
}
