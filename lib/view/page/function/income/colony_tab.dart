import 'package:flutter/material.dart';

/// 收入来源「殖民地」tab：输入框 + 实时计算详单（待补充）
class ColonyTab extends StatelessWidget {
  const ColonyTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('殖民地（待实现）', style: TextStyle(color: Colors.grey)),
    );
  }
}
